# Copy to .env.local and adjust

# API base URL — leave blank to use Vite proxy (recommended for local dev)
# Set to full URL if frontend and backend are on different origins in production
VITE_API_URL=

# Optional: override the app name shown in the browser tab
# VITE_APP_NAME=IFFAA Accounting System
